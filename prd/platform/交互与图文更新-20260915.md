# MVP-P0 交互与图文更新 · 2026-09-15

## 页面与文案

- 用量概览 / 请求明细的选中背景增加 220ms 滑动动画；键盘切换可用，减少动态效果偏好下关闭动画。
- Info 按当前位置解释实际口径：企业共享并发、用量筛选范围、历史峰值、实际扣积分请求、Key 安全与企业支付认证对象。
- 并发进度条保留绿色（低于 80%）、橙色（80% 至不足 100%）、红色（达到或超过 100%）。橙色与红色增加文字说明。
- 演示请求包含正常、34/40 接近上限和 30/30 达到上限样例，峰值仍由请求时间重叠计算。

## Figma 与 PRD

- API Keys、用量与积分、计费标准分别将企业和个人放在独立区域，保留原画板节点与交互连接。
- 通用组件区新增统一 Info 文案与动效说明。
- 飞书 PRD 的对应章节补充 15 张当前页面截图、4 张可编辑流程图：Key 创建、生命周期、企业并发接纳与释放、账单与 CSV 导出。
- 本地 PRD 同步图文，图片与 Mermaid 源码保存在 `assets/20260915`。

[飞书 PRD](https://acnc6zeentra.feishu.cn/docx/S9zbdxp89oGfXYxKWkTc22QAnef)

[Figma](https://www.figma.com/design/uKJHefil0SBm8xMnc37NnE)

## 验证

- 39 项业务检查通过；TypeScript 与 Next.js 生产构建通过。
- 浏览器验证页签点击、键盘切换与选中背景位置。
- 飞书回读确认 15 张图片、4 张画板；四张流程图已导出并目视检查。
- Figma 角色区域的原型目标节点有效。

## 补充修正：页签滚动与个人设计稿

- 用量概览 / 请求明细切换保留内容容器；内容变短时保留到当前视口底部的最小高度，避免浏览器向上修正滚动位置。继续使用 220ms 选中背景动画。
- 同步维护页签与内容面板的无障碍关联；鼠标和方向键切换均保持当前位置。
- 个人角色的用量与积分补齐请求明细、请求详情、账单预览、按模型与 Key 汇总导出、逐请求导出，共六个状态，独立于企业区域排列。
- 个人设计稿沿用已有 2026-09-07 至 2026-09-13 演示基线：84 条计费请求、39.30 积分；按个人标准价展示，不使用企业折扣。
- [个人角色 Figma 区域](https://www.figma.com/design/uKJHefil0SBm8xMnc37NnE?node-id=80-1104)。概览、明细、请求详情、账单、导出粒度切换和关闭路径已连接到个人画板。
- 回归验证：页面滚动到 481px 后，鼠标来回切换和方向键切换均保持 481px，页签与当前面板关联正确。

## 企业 Key 授权模型空选提示

- 企业创建和编辑 API Key 取消全部模型时，在模型列表下显示红字“请至少选择一个授权模型。”，确认 / 保存保持禁用。
- 重新勾选至少一个模型后立即移除提示；所有字段有效时允许提交。企业与个人采用相同空选口径，并为模型字段组关联错误说明。

## 飞书 PRD 小修同步

- 2026-09-15 已同步空选校验触发 / 恢复规则、页签滚动保持、按钮类型与状态、弹窗组件范围及个人六个设计状态。
- A01、A18 验收步骤与结果已更新；版本说明更新至代码 19fe707。
- 创建与编辑章节新增企业空选 Figma 状态图；原有页面图片、流程图及文档内人工批注样式保留。
- 飞书回读确认更新成功，文档版本 48。

## 并发 mock 四种状态

- 默认同时展示正常 41/100、50/80、21/40，零值 0/60，接近上限 34/40，达到上限 30/30。
- 监测 mock 使用当前分钟生成请求区间并计算峰值，避免首次访问的旧时间锚点使样例逐个滑出 24 小时窗口。
- 历史账单仍使用固定时间锚点，监测样例不进入计费。40 项业务检查和 TypeScript 通过；浏览器确认四种状态同时可见。
- 飞书 PRD 已同步上述演示规则。

## 积分弹窗遮罩统一

- 充值、购买、获取积分入口复用创建 Key 的 80% 黑色遮罩，移除积分弹窗单独的 45% 浅色遮罩分支。
- 浏览器验证背景为 rgba(0, 0, 0, 0.8)，飞书 PRD 同步该规则。

## 金额估算参考口径

- 明确 0.05 元/积分来自基础积分包 20 元 ÷ 400 积分，当前为固定展示参考值。
- 筛选后的消耗积分乘参考值计算金额，积分已含模型折扣；反算比值没有独立业务含义。
- 估算不代表真实充值成本，不作为结算、退款或开票依据；已同步飞书 PRD 并回读确认。

## 工作空间切换图文说明

- 在身份与权限章节补充线上原型的企业、个人账户菜单截图，分别说明“切换至个人空间”与“切换至星河科技”的入口和操作。
- 说明切换保留当前功能页面，更新目标空间的身份、积分与 Key 数据，同时重置草稿、筛选、分页和 URL 查询条件；已保存数据仍归属原空间。
- 截图位于 assets/20260915/workspace-enterprise-menu.png 与 workspace-personal-menu.png。
- 已同步飞书“7.1 身份与权限”，回读确认两张图片及操作说明完整，文档版本 55。

## Figma 对照线上修正

- 对照线上 2ab1d6e，企业费用中心六个画板及弹窗背景均同步并发样例 41/100、50/80、21/40、0/60、34/40、30/30；加入橙色、红色进度条及文字说明，零值保留空轨道。
- 企业个人充值、企业支付、个人充值三个画板的遮罩从 45% 统一至黑色 80%，与创建 Key 一致。
- 企业用量概览与请求明细的原型连线改为保留滚动，与个人版一致；线上 220ms 滑动动画仍由应用实现，目录中明确演示边界。
- 补充可复用账户菜单组件 118:41；新增企业工作空间切换 119:1271、个人工作空间切换 120:1373，打开、关闭和切换目标连线已检查。
- 更新金额参考口径、零值状态、空选校验、遮罩与工作空间交互说明；目录 API Keys 状态总数更新为 15。
- 已核对计费表列对齐、表头 #555555、企业空选提示和个人费用中心六个状态。画板字体保持 Noto Sans SC / Sora，账单样例保留 09-07 至 09-13 基线。
- 节点与验收记录见 figma-sync-audit-20260915.json；代表画板已检查渲染，无失效切换目标。

### 请求详情末行分割线修正

- 请求详情仅在字段之间显示分割线，末行不显示底线；有计费快照时末行为模型折扣，无快照时末行为消耗积分。
- 同步企业、个人请求详情及 Figma 通用请求详情组件：39:1913、98:1786、71:119。

### 工作空间切换设计稿补充

- 在 Figma「通用组件与提示」新增独立工作空间切换说明区（129:41），企业→个人、个人→企业分栏展示展开菜单、当前身份与切换结果。
- 明确保留当前功能页面、清空原空间筛选及未提交表单、已保存数据按空间隔离、刷新保留当前空间。
- 说明区链接至企业/个人完整切换画板（119:1271、120:1373）；原型的展开、收起及双向切换 6 条连接已核验。
- 线上参考：https://moss-api-mvp-p0-compromise.vercel.app/app/api-keys

### 工作空间切换独立分页面

- 新增 Figma 页面「全局导航 · 工作空间切换」（136:61），置于总目录之后。
- 双向流程画板 129:41 从通用组件页迁移到新页，重命名为「全局导航 / 工作空间切换 / 双向流程」；原有节点链接保持有效。
- 通用组件页保留账户入口与菜单组件，总目录补充独立页入口。API 密钥页的完整切换状态保留，用于页面原型演示。
