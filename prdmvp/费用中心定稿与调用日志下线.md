# 费用中心定稿与调用日志下线

日期：2026-09-09。此决定晚于“费用中心迭代完成、重复页面保留”标签，覆盖该阶段的页面保留约定。

## 选定页面

| 页面 | 当前地址 | 实现 | 粒度 |
| --- | --- | --- | --- |
| 费用概览 | `?view=billing3` | `app/v3/billing3-mvp.tsx` | 模型×用户 |
| 用量明细 | `?view=usage3` | `app/v3/usage3-mvp.tsx` | 模型×用户×API Key |

页面标题和页签不再带数字。保留实现文件及URL后缀，以维持已分享链接和明细上下文的兼容性。费用中心只显示这两个页签；Owner/Admin可访问两页，Developer侧栏费用中心进入用量明细，且只显示该页签与本人权限范围。

选择依据：费用页完成账期汇总与用户归因，明细页承担每日趋势及Key级核对；两页使用同一计量账本，保留已确认的费率、打印、导出、用户身份、分页和数值格式。旧版费用、用量及日志候选不再构成客户任务入口。

## 移除范围

- 调用日志模块完整下线，包括请求日志和服务质量全部候选页面。
- 费用中心删除原费用概览、费用候选2/3旧版/4/5/6，以及旧用量1–9候选实现，仅保留本轮选定的3方案。
- 删除13个独立候选组件文件、management中的旧Billing/Usage实现、无用路由状态和515条已无页面使用的候选样式规则。
- 删除首页、模型详情、Playground、项目/Key详情及正式API凭证中的日志/质量跳转按钮；不再展示目标为已下线模块的历史通知。
- 凭证与组织的既有页面保留，操作审计继续可用。共用的资源证据数据、图表基础组件、usage8数据函数和样式保留，不能因历史命名误删。

## 路由及交互

兼容规则集中于`app/v3/page-navigation.ts`，旧路由不再存在于活动View注册表或权限列表。

- `billing`、`billing2`、`billing4`–`billing6`、`orders`、`orderDetail` → `billing3`。
- `usage`、`usage2`、`usage4`–`usage9`、`usageDetail` → `usage3`。
- `logs`/`quality`及2–7后缀 → 平台首页，并替换URL。
- 用户用量概览“查看明细”继续传递账期、模型、用户；用量页校验权限，Key默认全部，窗口整账期。
- 切换Developer时，若原页面无权限则转入本人用量或首页，同时同步URL并清除旧下钻参数。
- 打印仍以完整企业账期按模型聚合并包含计费规则；CSV仍按完整授权账期导出Key级数据，页面筛选不会缩小账期导出范围。

## 文档与追溯

当前费用规则见[费用中心PRD](../old/prd-before-engineering-cleanup-20260911/PRD-费用中心.md)。prd中的旧费用/调用候选说明已删除，相关历史从Git查询；上一发布记录仅作追溯。

删除前代码已保存在GitHub提交`f419b10`及标签`management-organization-mvp-billing-iteration-complete-20260909`，可按需回看，当前不重新引入历史组件。
