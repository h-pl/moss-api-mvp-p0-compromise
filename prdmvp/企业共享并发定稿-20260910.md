# 企业与用户共享并发定稿（2026-09-10）

用户选定共享版“企业与用户2”为正式“企业与用户”，唯一在线入口为[企业与用户](https://moss-api-enterprise-console.vercel.app/?view=enterprise)。旧enterprise2链接归一到enterprise，原预留分配页面、独立页签和校验分支已移除。收敛前对比版本保存在Git提交92c29a2。内部enterprise4/eu2样式命名沿用，仅有一个正式组件。

| 决策 | 实现 | 文档 | 验证 |
|---|---|---|---|
| 同企业同模型共享池，成员上限之和可超过企业上限 | member-lifecycle.ts、user-policy-dialog.tsx | 主PRD7.6、7.7.1、7.10 | 多个成员均配置企业上限可保存；单项超限被拒 |
| 模型授权与可空成员上限分开 | user-policy-dialog.tsx、enterprise-resource-data.ts | 主PRD7.7.1、通用表单规范 | 未选禁用，勾选留空，0/负数/小数拒绝，null序列化保留 |
| Owner默认继承且可编辑，Admin保留企业权限 | mvp-account.ts、enterprise-mvp.tsx | 身份系统说明、主PRD7.1 | 浏览器Owner只读身份字段，Admin可编辑Developer而无Owner编辑入口 |
| 四页同源，保留存量演示数据 | mvp-storage.ts、mvp-pages.tsx、V3App.tsx | 系统说明、实现说明 | 合并两旧键，选定版本配置优先，旧独有记录保留，永久失效Key不恢复；重置清理三键 |
| 统一有效上限与历史峰值 | mvp-capacity.tsx | 主PRD7.7、指标说明 | Owner 68/80和50/60；同一时间线企业汇总；上限调低不改历史 |
| 单一入口和既有分页 | mvp-policy.ts、page-navigation.ts、pagination.tsx | 页面范围、UX-CONTRACT | 旧链接跳转；4页，默认5条、5/10/20 |
| 两列紧凑卡片与超额颜色 | user-policy-dialog.tsx、mvp.css | DESIGN、通用表单规范 | 桌面两列及窄屏单列，超限仅上限文字/输入边框红，无错误行撑高 |

共享池方案避免将闲置额度固化在某一成员名下；上限留空减少邀请配置量。保留原版的角色权限、邀请席位、5/10/20分页、移除Key永久失效及账务历史规则。并发预留方案不作为现行规则，不能按成员配置推导可分配余额，也不承诺成员独占并发。

阅读顺序：prd/README → 主PRD → 企业账号与资源治理 → 账号切换与企业资源 → 企业管理实现说明 → UX-CONTRACT/DESIGN。主PRD与prdmvp镜像一致；飞书原文档就地更新，保留问题Q-01至Q-06及功能分期表。成员流程图更新原Mermaid画板，七张相关截图已替换。

验证：45项MVP业务检查、8项用量检查、28项UI/结构检查通过；TypeScript、Next生产构建通过。ESLint无错误，management.tsx有8条既有警告。浏览器核对旧链接归一、Owner峰值/编辑、邀请可空及超额状态、详情、移除取消、API密钥资源同源、Admin企业访问与Developer配置权限。未发送真实短信或修改生产用户。

生产边界：真实鉴权、邀请7天到期/接受、网关实际并发原子计数与结束释放尚需后端；上限缩减与在途请求终止策略保留Q-05/Q-06待联调，不能以原型保存成功作为生产限制执行证据。
