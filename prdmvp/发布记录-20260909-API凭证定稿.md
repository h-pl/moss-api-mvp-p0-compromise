# API凭证迭代完成

2026-09-09凭证与授权定稿：选定API凭证2，正式名称为“API 凭证”，入口为 `?view=keys`；`?view=keys2`–`?view=keys8` 兼容归一到 `keys`。页签仅保留“API 凭证 / 音色授权”。采用 `credentials-mvp.tsx` 的 `ApiCredentialsMvp`，保留原API凭证2的资源摘要、本人Key管理及独立演示存储。原API凭证（候选7）组件和比较页签已移除，历史通过Git查询。

## 页面决策

- 明确采用用户选定的API凭证2，保留模型授权 → 我的 API Key → 本人本月已用积分 → 企业积分余量、近24h峰值/当前本人上限、80%/100%分类、展开收起持续提示，以及本人Key创建/启用/停用、搜索和10/20/50分页。
- 正式路由keys使用MvpPages与ApiCredentialsMvp；keys2–keys8兼容进入keys。音色授权voices不变。
- 删除旧credentials7.tsx组件和专属告警卡/筛选/进度样式；复用的credential7/credential6类名保留，代表样式来源。
- 同步prd索引、主PRD、资源治理、实现说明、UX、设计与MVP文档。研发先读prd/README.md和主PRD，再读凭证与授权实现说明。
- 沿用moss-api-mvp-demo-v1，不迁移原版keys的独立演示数据。生产鉴权、账务和审计联调不因本次页面定稿而视为完成。

## 验证与提交

已通过56项V3检查、API凭证正式路由与卡片顺序渲染回归、8项用量回归、ESLint、TypeScript及Next生产构建。浏览器确认旧keys2归一为keys、标题API凭证、仅两个页签、四项摘要及容量提示。提交仅包含API凭证定稿增量，工作区中的成员移除等独立改动不随本次提交。

## 文档同步

凭证实现提交 `56decb7d47d7dbe4457a9b35716942f086cbba1c`，定稿标签 `api-credentials2-final-20260909` 固定指向该提交。后续文档同步单独提交。

本地PRD已统一正式入口、两页签、调用资源顺序及80%/100%阈值，并纠正旧版轮换与审计实现描述。飞书同步至正文revision 503、内嵌表revision 436，回读通过；见[飞书同步记录](./飞书同步记录-20260909-API凭证定稿.md)。
