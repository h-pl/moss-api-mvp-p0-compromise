# 企业与用户迭代完成：选定企业与用户2

日期：2026-09-09。

## 用户确认与发布顺序

选定企业与用户2。按用户要求，先将仍保留两个版本的迭代成果提交并推送GitHub，确认远端保存后再清理旧版；清理另作提交。该记录保留发布顺序，不代替最终路由说明。

## 本轮结果

- 企业资源池顺序：企业积分余量、已开通模型、可邀请席位。
- 总席位50，正常和待接受占用席位；已加入7位、无待接受时显示43 / 50席。界面与邀请校验共用上限。
- 移除用户后从用户与配额、搜索、筛选和计数中消失，并释放占用席位；同一手机号可重新邀请，按新授权加入。
- 历史用户身份、Key元数据、费用、用量及归属保留；原企业Key永久失效，个人Key不受影响。永久失效的历史Key不占新关系的创建额度。
- 用户详情仅显示已授权模型，未授权项在编辑/邀请时供选择；保留授权数/企业模型总数。
- 用户详情保留企业Key只读列表，仅名称/掩码、状态、最近调用三列，删除有效期。
- 邀请仍为本地演示，不发送短信；真实受邀人接受及网关撤权待后端联调。

选定原因：用户移除和重新邀请形成明确管理流程；成员治理、调用授权和历史记账分别处理；详情聚焦当前授权与只读Key信息，资源池与邀请校验使用一致的50席上限。

## 清理与兼容

清理前提交 `f41aa3840f56b21aeef548cae58946befeaf574b` 已推送 `origin/main` 并核对远端一致。随后删除旧企业成员组件及其停用/恢复弹窗、旧详情/编辑路径，正式路由统一为 `enterprise`。保留浏览器MVP存储键与历史身份目录，旧地址兼容跳转。

PRD、系统说明、实现说明、UX、设计和文档索引已同步；飞书正文、两段状态机及50格需求表已回读验证（修订540）。本轮31项MVP、8项用量、56项页面契约、TypeScript、变更文件lint和生产构建通过；浏览器核对兼容路由、50席和详情字段。
