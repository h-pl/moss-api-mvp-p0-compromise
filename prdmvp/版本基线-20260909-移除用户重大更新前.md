# 移除用户重大更新前基线

日期：2026-09-09。标签：`pre-user-removal-major-update-20260909`。

本标签保存从“停用/恢复账户”改为“移除用户”之前的完整页面和代码状态，作为重大行为变更的比较及回退基线；不表示移除用户逻辑已经实现。

## 基线范围

- 企业认证已选定原认证2，只读主体信息，正式入口security；未选定登录用户会话管理。
- 企业资源池摘要顺序为企业积分余量、已开通模型、可邀请席位。
- 当前企业与用户、API凭证的资源提示、近24h峰值与当前并发上限、接近上限筛选、精简表格，以及费用摘要和凭证摘要顺序一并纳入快照。
- 原企业与用户页保留停用/恢复行为；MVP企业与用户2采用停用且不支持自助恢复。两套现有行为均保留在该标签中。
- 对应PRD、设计、UX、实现说明及既有飞书同步记录一并保存；保存同步记录不代表本次重新同步飞书。

## 下一阶段定位

下一阶段将账户停用/恢复改为移除用户，按重大行为变更处理。成员关系、企业Key处置、历史用量/账务归属、再次邀请及权限边界需要随新逻辑统一定义与验收。本基线只保存变更前状态，不预先改写这些规则。

## 验证

快照已通过24项MVP、8项计量和56项V3回归（共88项）、TypeScript及Next webpack生产构建。全量ESLint为0错误、13条既有警告，位于未修改的dialogs.tsx和management.tsx。
